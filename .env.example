# Empty value uses same-origin /api proxy or hosting rewrites.
VITE_API_BASE_URL=

# Use an absolute URL only when that backend allows browser CORS.
# VITE_API_BASE_URL=https://your-backend.example.com
